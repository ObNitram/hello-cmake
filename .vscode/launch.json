{
    "configurations": [
        {
            "type": "lldb-dap",
            "request": "launch",
            "name": "DebugCustom",
            "program": "${workspaceFolder}/build.clang.debug/MyProject",
            "env": {
                "ASAN_OPTIONS": "detect_leaks=0",
                "LLVM_SYMBOLIZER_PATH": "/usr/bin/llvm-symbolizer-18"
            }
        }
    ]
}